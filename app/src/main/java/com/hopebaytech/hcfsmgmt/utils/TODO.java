package com.hopebaytech.hcfsmgmt.utils;

public class TODO {
    // TODO Circular progress bar persists after pin fails (Use bindService to connect with Service for pin failed callback function)
    // TODO Pin/Unpin 時如果選擇的項目超出畫面, 則Pin/Unpin後項目icon不會更新
    // TODO It should have a message when back end server fail to connect.
    // TODO Android folder is unpin by default but user cannot unpin after pin Android folder, user feel confuse
    // TODO Non-intuitive behavior when pinning fails due to no enough cache space

    /**
     * Done:
     * 1. Default display layout is changed to Grid layout
     * 2. Remove "Display by data type"
     * 3. Tap App icon to pop up detail information dialog both in ListView and GridView
     * 4. Fix incorrect app/file local data ratio
     * 5. Build database even TeraFonn is not activated
     * 6. Change targetSdkVersion to 23 and add READ_EXTERNAL_STORAGE runtime permission
     * 7. Add dialog to show local data ratio when user long-press on file/dir item
     * 8. Add pin/unpin action icon to item popup dialog
     * 9. Add IMEI and system version information to about page
     * 10. Display colorful/gray item icon
     * 11. Apply the value of (pin + unpin but dirty) to local space usage notification
     * 12. Apply hcfs connection status api on dashboard and ongoing notification
     * 13. Fixed bug 10590, 11138, 11155, 11100, 11097, 9861, 11178, 11172, 11166 and 11161
     */
}
